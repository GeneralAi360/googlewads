# v0.2 Release Gate

`dev/performance-banner-designer-v0.2` is an active release-candidate hardening branch, not a completed release.

Real Work acceptance has now produced two permanent regression classes:

- `REAL-01` — premature rendering without category/competitor research, superficially different art directions, generic/toy-like B2B imagery and insufficient professional category fit;
- `REAL-02` — a much stronger research-first written direction that still drifted into unapproved CTA wording and ambiguous brand identity while correctly requiring real product UI.

## Deterministic engineering gates

- [x] structured intake and ambiguity handling
- [x] immutable run freeze and banner matrix
- [x] supplied-reference analysis contracts
- [x] competitive creative intelligence methodology and schema
- [x] competitor research task materializer
- [x] explicit `FULL / DEGRADED` research-rigor reporting
- [x] performance-evidence tiers that prevent unsupported "high-converting" claims
- [x] category design map schema
- [x] detailed design-brief schema with asset-quality policy
- [x] commercial-message lock with approved CTA allowlist
- [x] canonical brand-identity lock
- [x] required-asset contract with generated-substitute policy
- [x] representative asset-manifest schema and fail-closed asset validator
- [x] written art-direction approval schema
- [x] high-fidelity representative-design approval schema
- [x] preproduction freeze binding research → category map → design brief → commercial/brand locks → written approval → representative artifact
- [x] creative freeze linkage to preproduction-approved art direction, proposition, CTA allowlist and brand ID
- [x] one job per final output
- [x] deterministic exact-size PNG/JPG renderer
- [x] lighting/contrast/crop/clearspace gates
- [x] Google technical preflight
- [x] provenance manifest/contact sheet
- [x] design-QA diagnostic views
- [x] hash-bound banner/pack review-task materialization
- [x] final readiness evaluator
- [x] hidden-key visual-review calibration harness
- [x] canonical seven-format E2E updated to include preproduction locks
- [x] REAL-01 regression contract
- [x] REAL-02 regression contract

## Required real Work re-test

Current Work validation has already demonstrated:

- [x] research is attempted before unresolved art-direction rendering;
- [x] unavailable paid-ad-library evidence is reported honestly as `RESEARCH_RIGOR = DEGRADED`;
- [x] Work can produce a materially stronger written B2B direction without rendering prematurely;
- [x] Work recognizes that real product UI must not be replaced by generated/fake UI.

Still required:

- [ ] Re-run from the updated `SKILL.md` and verify unapproved CTA wording is rejected instead of silently introduced.
- [ ] Verify exactly one canonical brand/display name is resolved before representative rendering.
- [ ] Verify `representative-asset-manifest.json` + `validate_representative_assets.py` returns `NEEDS_ASSET` when real UI/logo requirements are unmet.
- [ ] After required assets are supplied, render exactly one high-fidelity representative design.
- [ ] Verify rejected representative design returns to art direction/design brief instead of multiplying weak work.
- [ ] Approve one representative artifact before full-pack scale-out.

## Independent rigor gates

- [ ] Six hidden-key visual eval cases executed by genuinely fresh visual reviewer contexts and scored.
- [ ] Final repository/PR reviewed by a genuinely independent reviewer context.
- [ ] Important independent findings reconciled and CI re-run.

## Merge rule

Do not merge into `main` merely because deterministic CI is green.

Merge only when:
1. deterministic CI is green on the final head;
2. the real Work re-test above passes;
3. independent rigor gates are satisfied, **or** the user explicitly accepts degraded rigor;
4. the user explicitly approves merge.

Until then:
- deterministic engineering: `ACTIVE_HARDENING`;
- real user-acceptance validation: `RETEST_REQUIRED`;
- delivery/runtime rigor: `NOT_FULLY_VALIDATED`;
- merge: `BLOCKED_BY_RELEASE_GATE`.
