# v0.2 Release Gate

`dev/performance-banner-designer-v0.2` is an active release-candidate hardening branch, not a completed release.

Real Work acceptance has produced two permanent regression classes:

- `REAL-01` — premature rendering without category/competitor research, superficially different art directions, generic/toy-like B2B imagery and insufficient professional category fit;
- `REAL-02` — a much stronger research-first direction that still drifted into unapproved CTA wording and ambiguous brand identity while correctly requiring real product UI.

The user-supplied visual-methodology presentation added another deterministic requirement: the pipeline must freeze **idea → presentation → emotion → visual character → lighting intent** before art direction and must freeze a campaign design system before scale-out.

## Deterministic engineering gates

- [x] structured intake and ambiguity handling
- [x] immutable run freeze and banner matrix
- [x] supplied-reference analysis contracts
- [x] competitive creative intelligence + A–E evidence tiers
- [x] explicit `FULL / DEGRADED` research-rigor reporting
- [x] category design map
- [x] `IDEA_ARCHITECTURE` contract
- [x] presentation-mode vocabulary
- [x] emotional target / avoided-tone contract
- [x] extensible `VISUAL_CHARACTER` signature and two character axes
- [x] focus budget
- [x] global/brand/concept forbidden-visual layers
- [x] Creative Chaos Audit
- [x] optional Creative Disruption Library with explicit hypothesis status
- [x] commercial-message lock / CTA allowlist
- [x] canonical brand-identity lock
- [x] required-asset contract and `NEEDS_ASSET` validator
- [x] `LIGHTING_INTENT` derived from idea/emotion/character/AOI
- [x] scene-light `REQUIRED / OPTIONAL / NOT_APPLICABLE` semantics
- [x] composition-light primitive whitelist
- [x] real-UI rule preventing fake relighting
- [x] written art direction bound to exact idea/character/lighting identity
- [x] structured hero-generation spec with generated critical text/logo prohibited
- [x] high-fidelity representative approval requires idea/emotion/character/lighting fidelity
- [x] `CAMPAIGN_DESIGN_SYSTEM` after representative approval
- [x] campaign lighting may narrow but never expand lighting-intent primitives
- [x] preproduction freeze binds research → category → meaning → character → lighting → art direction → representative → campaign system → matrix
- [x] creative freeze linkage to preproduction proposition/CTA/brand/art direction/design system
- [x] one job per final output
- [x] deterministic exact-size PNG/JPG renderer
- [x] Google technical preflight
- [x] provenance manifest carries campaign system / idea / character / lighting IDs
- [x] design-QA diagnostic views
- [x] independent banner review requires semantic + lighting fidelity
- [x] pack review requires cross-size idea/emotion/character/design-system/lighting consistency
- [x] final readiness evaluator
- [x] hidden-key visual-review calibration harness
- [x] canonical seven-format E2E expanded through meaning/lighting/design-system freeze
- [x] REAL-01 regression contract
- [x] REAL-02 regression contract

## Required real Work acceptance

Already demonstrated:

- [x] research happens before unresolved design rendering;
- [x] unavailable paid-ad-library evidence is honestly reported as `RESEARCH_RIGOR = DEGRADED`;
- [x] Work can create a substantially stronger written B2B direction without rendering prematurely;
- [x] Work recognizes real product UI should not be replaced by fake/generated UI.

Still required on the latest skill:

- [ ] verify unapproved CTA wording is rejected;
- [ ] verify one canonical brand/display name is resolved;
- [ ] verify asset manifest + validator returns `NEEDS_ASSET` when UI/logo are missing;
- [ ] after assets are supplied, resolve current MITGROUP `IDEA_ARCHITECTURE`, visual character and lighting intent;
- [ ] render exactly one high-fidelity representative 300x250;
- [ ] verify representative passes idea, emotion, character and lighting-intent fidelity;
- [ ] freeze `campaign-design-system.json`;
- [ ] verify rejected representative returns to idea/art direction instead of scale-out;
- [ ] only after representative approval, test the complete multi-size pack.

The user's upcoming banner-style examples must also be evaluated. They should extend `VISUAL_CHARACTER / style_tags` and reference DNA rather than become unconditional templates.

## Independent rigor gates

- [ ] six hidden-key visual eval cases executed by genuinely fresh visual reviewer contexts and scored;
- [ ] final repository/PR reviewed by a genuinely independent reviewer context;
- [ ] important independent findings reconciled and final-head CI re-run.

## Merge rule

Do not merge into `main` merely because deterministic CI is green.

Merge only when:
1. deterministic CI is green on the final head;
2. the real Work acceptance gates above pass;
3. independent rigor gates are satisfied, **or** the user explicitly accepts degraded rigor;
4. the user explicitly approves merge.

Until then:
- deterministic engineering: `ACTIVE_HARDENING`;
- real user-acceptance validation: `RETEST_REQUIRED`;
- delivery/runtime rigor: `NOT_FULLY_VALIDATED`;
- merge: `BLOCKED_BY_RELEASE_GATE`.
