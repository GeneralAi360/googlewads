# v0.2 Release Gate

`dev/performance-banner-designer-v0.2` is an active release-candidate hardening branch, not a completed release.

Real Work acceptance has produced five permanent regression classes:
- `REAL-01` — premature rendering without category/competitor research, superficially different art directions, generic/toy-like B2B imagery and insufficient professional category fit;
- `REAL-02` — a stronger research-first direction that still drifted into unapproved CTA wording and ambiguous brand identity while correctly requiring real product UI;
- `REAL-03` — a correct `NEEDS_ASSET` result was incorrectly treated as a global stop and skipped upstream IDEA_ARCHITECTURE / VISUAL_CHARACTER / Style Intelligence / attention / typography / LIGHTING_INTENT work;
- `REAL-04` — the user clarified that the approval artifact must be the rendered visual concept itself, not a text-only art-direction description;
- `REAL-05` — production asset blockers still caused the system to show no actual banner concept to the user, even after strategy had completed.

Canonical user-facing production intent is now:

`QUESTIONS / INTAKE → STRATEGY → INTERNAL ART-DIRECTION FREEZE → RENDERED VISUAL CONCEPT → USER APPROVAL → PRODUCTION ASSET COMPLETION IF NEEDED → PRODUCTION REPRESENTATIVE → CAMPAIGN DESIGN SYSTEM → FULL PRODUCTION`.

`references/gate-order-and-blocking-boundaries.md` makes the asset-blocking scope explicit. `references/visual-concept-approval.md` makes both the user-approval boundary and the new concept-preview-with-asset-slots path explicit.

## Deterministic engineering gates

- [x] structured intake / immutable matrix
- [x] reference + competitive/category research
- [x] idea/presentation/emotion/visual-character contracts
- [x] September 2026 Style Intelligence, attention and typography strategy
- [x] capped/stale-aware currentness + three strategy lanes + drift gate
- [x] focus budget / forbidden visuals / Creative Chaos Audit
- [x] commercial/CTA + brand identity locks
- [x] real-asset / `NEEDS_ASSET` production gate
- [x] explicit asset-gate blocking boundary: not upstream strategy
- [x] explicit concept-preview boundary: production `NEEDS_ASSET` does not hide the user-facing design
- [x] pre-render Google policy-risk phase
- [x] meaning/style/AOI-linked `LIGHTING_INTENT`
- [x] internal written art-direction freeze before user visual approval
- [x] rendered visual concept as the primary user approval artifact
- [x] two approval scopes: `EXACT_PRODUCTION_ARTIFACT` and `VISUAL_SYSTEM_WITH_ASSET_SLOTS`
- [x] safe concept-only asset modes: authentic low-res/reference surrogates, structural placeholder, text-brand placeholder
- [x] fake/generated UI and fake logos forbidden even in concept preview
- [x] concept preview with surrogates stays `NEEDS_ASSET` + `not_for_delivery=true`
- [x] concept preview must include a user-facing concept card
- [x] exact-SHA user decision model: `APPROVE / REVISE / REJECT`
- [x] exact-artifact `APPROVE` path can unlock ordinary scale-out gates
- [x] visual-system `APPROVE` becomes `VISUAL_CONCEPT_APPROVED_ASSET_PENDING`
- [x] user-rooted system-fidelity gate supports no-second-approval continuation only when real asset substitution causes no material drift
- [x] `MATERIAL_DRIFT` requires a new user visual approval cycle
- [x] one production representative / campaign design system / scale-out boundary
- [x] preproduction + creative freeze / one job per output
- [x] deterministic exact-size renderer
- [x] Google technical preflight
- [x] dated Google Ads policy snapshot + freshness rule
- [x] exact-artifact policy context/report schemas
- [x] exact-SHA `GOOGLE_POLICY_REVIEWER` semantic review contract
- [x] misleading-design / transparent-background / claims / destination / identity / trademark / restricted-vertical policy gates
- [x] pack-level exact-SHA policy aggregation
- [x] final `GOOGLE_READY_PRECHECK_PASS`
- [x] no-Google-approval-guarantee rule
- [x] visual diagnostics / independent design-review contracts
- [x] hidden-key visual-review harness
- [x] REAL-01 / REAL-02 / REAL-03 / REAL-04 / REAL-05 regressions

## Visual concept approval acceptance gate

Before any real full banner pack is produced:
- [ ] intake asks only the business/design questions necessary to create the campaign correctly;
- [ ] strategy is resolved internally before rendering;
- [ ] one rendered visual concept is shown to the user, normally 300x250 unless another format is more representative;
- [ ] the user sees the image **and** a short concept card, not merely status/JSON;
- [ ] if production assets are ready, the concept may use `EXACT_PRODUCTION_ARTIFACT` scope;
- [ ] if production assets are missing, the system still attempts a safe `VISUAL_SYSTEM_WITH_ASSET_SLOTS` preview;
- [ ] concept-only surrogates are explicitly declared and never misrepresented as production-ready;
- [ ] the exact visual preview is bound by SHA-256;
- [ ] user decision is explicitly captured as `APPROVE`, `REVISE`, or `REJECT`;
- [ ] `REVISE` causes a new rendered version and a new approval cycle;
- [ ] `REJECT` returns to the smallest responsible upstream design/strategy layer;
- [ ] `APPROVE` on an asset-slot preview approves the visible design system only and does not bypass production asset requirements;
- [ ] after real assets arrive, production representative fidelity to the approved visual system is checked;
- [ ] fidelity `PASS` may continue without a second user approval; `MATERIAL_DRIFT` must be shown again;
- [ ] no full pack is produced before either exact-artifact approval or user-approved-system-plus-fidelity gate passes.

## Google Ads policy acceptance gate

Before a real pack is called `Google-ready`:
- [ ] current English Google Advertising Policies are checked or local snapshot is fresh;
- [ ] every final banner has an exact-SHA `GOOGLE_POLICY_REVIEWER` result;
- [ ] no misleading system/dialog/pseudo-control/download-install/pseudo-interaction/segmented/transparent-background issue;
- [ ] image/text quality is policy-acceptable;
- [ ] material claims and required qualifiers are verified;
- [ ] real landing page supports exact offer and CTA;
- [ ] destination working/domain/crawlability/geo/accessibility evidence is resolved;
- [ ] advertiser identity and affiliation claims are verified;
- [ ] trademark use is contextually resolved;
- [ ] restricted vertical/certification/targeting states are resolved when applicable;
- [ ] every report and pack aggregation is `GOOGLE_POLICY_PREFLIGHT_PASS`;
- [ ] final local status is `GOOGLE_READY_PRECHECK_PASS`;
- [ ] delivery still states final Google review applies and approval is not guaranteed.

Any `GOOGLE_POLICY_PREFLIGHT_BLOCKED`, `GOOGLE_POLICY_PREFLIGHT_INCOMPLETE`, or `POLICY_REVIEW_REQUIRED` blocks a Google-ready claim.

## Required real Work acceptance

Already demonstrated:
- [x] research before unresolved rendering;
- [x] honest `RESEARCH_RIGOR = DEGRADED` when paid-ad evidence is unavailable;
- [x] stronger written B2B direction without premature rendering;
- [x] real UI is not silently replaced by generated fake UI;
- [x] real MITGROUP commercial lock resolves to `Битрикс24 / Внедрение Битрикс24 / Оставить заявку` without inventing a promotional claim;
- [x] real MITGROUP brand lock keeps canonical `MITGROUP` and does not silently substitute `MitLab`;
- [x] Bitrix24 affiliation claim is held at `NONE` rather than invented;
- [x] pre-render Google policy-risk review reaches `PRE_RENDER_POLICY_PASS` after unsupported offer/qualifier claims are removed;
- [x] production asset readiness correctly returns `NEEDS_ASSET` for missing real Bitrix24 UI and production MITGROUP logo;
- [x] REAL-03 exposed that this downstream asset state must not skip upstream strategy;
- [x] REAL-04 established that the user approves a rendered visual concept rather than a written art-direction document;
- [x] REAL-05 exposed that production `NEEDS_ASSET` must not leave the user with no visual concept to judge.

Still required:
- [ ] re-run MITGROUP on the REAL-05 logic and prove that strategy can complete while production assets remain missing;
- [ ] render and show a real 300x250 concept preview despite `PRODUCTION_ASSET_READINESS = NEEDS_ASSET`, using only safe declared surrogate/slot treatment;
- [ ] show a concise concept card alongside that visual;
- [ ] capture explicit user `APPROVE / REVISE / REJECT` on the shown preview;
- [ ] if approved while assets are pending, request only the remaining production assets;
- [ ] supply and validate real Bitrix24 UI + production MITGROUP logo;
- [ ] materialize the production representative from the approved visual system;
- [ ] prove fidelity `PASS` can continue without redundant second approval and material drift cannot;
- [ ] only then freeze campaign design system and test full pack;
- [ ] final exact-artifact Google policy preflight using real MITGROUP landing page and Bitrix24 advertiser/trademark context.

Current real-run status:
- `COMMERCIAL_LOCK = COMMERCIAL_LOCKED`;
- `CTA_STATUS = LOCKED`;
- `BRAND_STATUS = BRAND_IDENTITY_LOCKED_LOGO_ASSET_MISSING`;
- `BITRIX24_AFFILIATION_STATUS = NONE`;
- `PRE_RENDER_POLICY_STATUS = PRE_RENDER_POLICY_PASS`;
- A2 / Product Reality strategy has completed;
- `PRODUCTION_ASSET_READINESS = NEEDS_ASSET`;
- the last Work run incorrectly showed **no rendered visual concept**;
- user visual approval has **not** occurred;
- full production remains blocked.

## Latest deterministic verification

**183 tests — OK** on the REAL-05 concept-preview / user-rooted asset-substitution fidelity head.

GitHub Actions **PASS** after adding:
- `visual-concept-preview` schema/validator;
- dual approval-scope semantics;
- safe asset-slot preview rules;
- one-approval system-fidelity scale-out path;
- REAL-05 regression coverage.

This proves deterministic logic, not real-market performance, independent visual judgment or actual Google approval.

## Next style-example integration

Upcoming user examples are analyzed as `REFERENCE_DNA` and reusable foundation/overlay/execution/attention/typography/lighting vocabulary or one-off devices. No style is encoded as high-converting without verified performance evidence.

## Independent rigor gates

- [ ] six hidden-key visual evals executed by genuinely fresh reviewer contexts;
- [ ] genuinely independent final repository/PR review;
- [ ] important findings reconciled and final-head CI re-run.

## Merge rule

Do not merge because deterministic CI is green alone.

Merge only after real Work acceptance, visual-concept user approval flow, real-pack Google policy acceptance, style-example evaluation, independent rigor (or explicit degraded-rigor acceptance), and explicit user approval.

Until then:
- deterministic engineering: `ACTIVE_HARDENING`;
- real user-acceptance validation: `RETEST_REQUIRED`;
- visual approval readiness: `REAL_VISUAL_CONCEPT_REQUIRED`;
- Google policy readiness: `REAL_PACK_VALIDATION_REQUIRED`;
- merge: `BLOCKED_BY_RELEASE_GATE`.
